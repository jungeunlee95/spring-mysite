<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<select id="getList" resultType="boardvo">
	<![CDATA[
		select no, title, author, view_count as viewCount, reg_date as regDate 
		from board 
		order by reg_date desc
	]]>
	</select>

	<insert id="insert" parameterType="boardvo">
	<![CDATA[
		insert into board values(null, #{title }, #{author }, #{content }, 0, now())
	]]>
	</insert>
	
	<select id="getView" parameterType="long" resultType="boardvo">
	<![CDATA[
		select no, title, author, content, view_count as viewCount, reg_date as regDate 
		from board 
		where no = #{no }
	]]>
	</select>
	
	<update id="update" parameterType="boardvo">
	<![CDATA[
		update board set title = #{title }, content = #{content } 
		 where no = #{no }
	]]>
	</update>
</mapper>
